#include <string>
#include <iostream>

int main()
{

    std::string S,T;
    getline(std::cin, S);
    std::stringstream X(S);
    while (getline(X, T, ' ') 
    {
        cout << T << endl;
    }

    return 0;
}
